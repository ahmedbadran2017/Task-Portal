from setuptools import setup, find_packages

setup(
    name="task_hub",
    version="0.0.1",
    description="Task Hub — cross-department task & issue management for Justyol portals",
    author="Justyol",
    author_email="info@justyol.com",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
